# MONGODB_CONNECTION_STRING FORMAT = "mongodb://${USERNAME}:${PASSWORD}:${PORT}:${DB_NAME}?authSource=admin"
# connection_string = "mongodb://max:secret@mongodb100:port/database?authSource=admin"

 docker-compose up --build -d 